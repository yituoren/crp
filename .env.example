# 服务端环境变量（systemd 部署时写在 deploy/crp.service 里）
PORT=3000
DATA_DIR=./data
# JWT_SECRET=留空则首次启动自动生成并保存到 DATA_DIR/jwt-secret.txt
# HOST_USERNAMES=阳秋,云缨,云影   # 首次初始化时的主办名单
# EVENT_NAME=BJ20 全明星赛        # 首次初始化时的赛事名
